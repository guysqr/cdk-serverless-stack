{
  "name": "blank-nodejs",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "aws-sdk": "2.631.0",
    "jest": "25.4.0"
  },
  "dependencies": {
    "aws-xray-sdk-core": "1.1.2",
    "html-creator": "^0.4.4"
  },
  "scripts": {
    "test": "jest",
    "build": "echo \"The build step is not required when using Javascript!\" && exit 0"
  }
}
